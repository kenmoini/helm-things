apiVersion: v1
kind: Service
metadata:
  name: {{ include "namespace-as-a-service.fullname" . }}
  labels:
    {{- include "namespace-as-a-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "namespace-as-a-service.selectorLabels" . | nindent 4 }}
