apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespace-as-a-service.fullname" . }}-test-connection"
  labels:
    {{- include "namespace-as-a-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namespace-as-a-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
